# Learning curve v2 — does train-set size limit validation F1?

_Auto-generated by `learning_curve_v2.py`, last update: 2026-08-14T17:42:39.419890+00:00_

## Question

Every prior training run used the full gemma_v2 train split (1985 rows / 800 groups), so the dataset-size → validation-performance relationship was unmeasurable from existing results. This experiment trains the **final locked LoRA config** (`lr=3e-4, r=8, alpha=16, dropout=0.0` — the Phase 2 / multiseed winner) on nested, group-aware, label-stratified subsets of `train.jsonl` and evaluates each on the same untouched 259-row validation split. The held-out test split was never touched.

## Method

- Subsets: nested (100 ⊂ 200 ⊂ 400 ⊂ 600 ⊂ 800 groups), stratified 50/50 at group level (groups are label-pure), membership fixed by `SUBSET_SEED=42` — identical subsets across training seeds.
- Training/eval pipeline: imported unchanged from `sweep_lora_v2_phase2.py` (same tokenizer, `PROMPT_1`, head-tail truncation, F1 early stopping, greedy generation).
- The two 800-group points are **read-only references** of already-completed runs with identical config+data+seed (Phase 2 seed 42, multiseed seed 23) — not retrained.
- Seed 23 at 200 groups (plus the existing seed-23 800-group reference) gives a seed-noise reading at two curve points.

## Results (validation, 259 rows)

| n_groups | n_rows | seed | best_epoch | precision | recall | F1 | invalid | source |
|---:|---:|---:|---:|---:|---:|---:|---:|---|
| 200 | 495 | 23 | 7 | 0.9264 | 0.9437 | **0.9350** | 0.000 | trained_here |
| 800 | 1985 | 23 | 4 | 0.9627 | 0.9688 | **0.9657** | 0.000 | reference_multiseed |
| 100 | 250 | 42 | 6 | 0.8970 | 0.9250 | **0.9108** | 0.000 | trained_here |
| 200 | 495 | 42 | 6 | 0.9419 | 0.9125 | **0.9270** | 0.000 | trained_here |
| 400 | 987 | 42 | 6 | 0.9618 | 0.9437 | **0.9527** | 0.000 | trained_here |
| 600 | 1477 | 42 | 3 | 0.9801 | 0.9250 | **0.9518** | 0.000 | trained_here |
| 800 | 1985 | 42 | 6 | 0.9808 | 0.9563 | **0.9684** | 0.000 | reference_phase2 |

## Analysis (seed-42 curve)

- 250 → 495 rows (×1.98): ΔF1 = **+0.0162**
- 495 → 987 rows (×1.99): ΔF1 = **+0.0257**
- 987 → 1477 rows (×1.50): ΔF1 = **-0.0009**
- 1477 → 1985 rows (×1.34): ΔF1 = **+0.0166**

Log-linear fit `F1 ≈ 0.7627 + 0.0186·log2(n_rows)` over 5 points → **+0.0186 F1 per doubling of data**.
- Naive extrapolation to ×2 data (3970 rows): F1 ≈ 0.9845 (caution: power-law extrapolation from 5 noisy points)
- Naive extrapolation to ×4 data (7940 rows): F1 ≈ 1.0031 (caution: power-law extrapolation from 5 noisy points)

## Seed noise check

- 200 groups: seed42 F1 0.9270 vs seed23 F1 0.9350 (Δ -0.0080)
- 800 groups: seed42 F1 0.9684 vs seed23 F1 0.9657 (Δ +0.0026)

Multiseed context (full data): Config A seed std was ±0.0032 F1 — deltas within ~±0.005 are indistinguishable from seed noise.

## Verdict

**The curve is still rising at full data** (+0.0186 F1/doubling, last segment +0.0166). Scaling this dataset further (more of the same distribution) would likely still help.


Caveats: validation is only 259 rows (1 FN ≈ 0.6pp recall), so differences under ~0.01 F1 are weak evidence; val→test ranking inversions have occurred before at this val size.
